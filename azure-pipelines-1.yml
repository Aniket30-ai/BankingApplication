trigger:
 - main

stages:
  - stage: Build
    jobs:
    - job: Build
      pool:
       vmImage: 'ubantu-latest'
      steps:
      - task: Maven@4
        inputs:
         mavenPomFile: 'pom.xml'
         goals: 'clean install'
